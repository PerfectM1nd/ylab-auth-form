import { Layout } from "../components/Layout";
import { LoginForm } from "../components/LoginForm.tsx";

export const Login = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};
